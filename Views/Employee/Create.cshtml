@model EmployeeAdminApp.Models.Employee

@{
    Layout = "_Layout";  // Ensure this page uses the layout file
}

@section Title
{
    Create Employee
}

<div class="container mt-5">
    <h2 class="text-center mb-4">Add New Employee</h2>

    <form asp-action="Create" method="post" class="needs-validation" novalidate>
        @* This will show if there are any validation errors when the form is submitted *@
        <div class="form-group mb-3">
            <label for="FirstName">First Name</label>
            <input type="text" class="form-control" id="FirstName" asp-for="FirstName" placeholder="Enter first name" required />
            <div class="invalid-feedback">
                Please enter a first name.
            </div>
            <span asp-validation-for="FirstName" class="text-danger"></span>
        </div>

        <div class="form-group mb-3">
            <label for="LastName">Last Name</label>
            <input type="text" class="form-control" id="LastName" asp-for="LastName" placeholder="Enter last name" required />
            <div class="invalid-feedback">
                Please enter a last name.
            </div>
            <span asp-validation-for="LastName" class="text-danger"></span>
        </div>

        <div class="form-group mb-3">
            <label for="Position">Position</label>
            <input type="text" class="form-control" id="Position" asp-for="Position" placeholder="Enter position" required />
            <div class="invalid-feedback">
                Please enter a position.
            </div>
            <span asp-validation-for="Position" class="text-danger"></span>
        </div>

        <div class="form-group mb-3">
            <label for="Salary">Salary</label>
            <input type="number" class="form-control" id="Salary" asp-for="Salary" placeholder="Enter salary" required />
            <div class="invalid-feedback">
                Please enter a salary.
            </div>
            <span asp-validation-for="Salary" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary btn-lg btn-block">Save Employee</button>
        <a href="@Url.Action("Index", "Employee")" class="btn btn-secondary btn-lg btn-block mt-3">Back to Employee List</a>
    </form>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"></script>
    <script>
        // Enable Bootstrap validation feedback
        (function () {
            'use strict'
            var forms = document.querySelectorAll('.needs-validation')

            Array.prototype.slice.call(forms)
                .forEach(function (form) {
                    form.addEventListener('submit', function (event) {
                        if (!form.checkValidity()) {
                            event.preventDefault()
                            event.stopPropagation()
                        }
                        form.classList.add('was-validated')
                    }, false)
                })
        })()
    </script>
}

